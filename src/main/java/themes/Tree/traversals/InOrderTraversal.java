package main.java.themes.Tree.traversals;

public class InOrderTraversal {
    /*
      public List<Integer> inorderTraversal(TreeNode root) {
        var order = new ArrayList<Integer>();

        if(root != null) {
            traverseInOrder(root, order);
        }

        return order;
    }

    private void traverseInOrder(TreeNode n, ArrayList<Integer> order) {
        if(n.left != null) {
            traverseInOrder(n.left, order);
        }

        order.add(n.val);

        if(n.right != null) {
            traverseInOrder(n.right, order);
        }
    }
     */
}
